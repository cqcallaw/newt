# test basic foreach
list {
	data:int,
	next:list?
}

l:= @list with { data = 2, next = @list with { data = 1 } }

fn:= ()-> int {
	for i in l {
		print("i: " + i.data)
		if(i.data == 2) {
			return i.data
		}
	}
	
	return 0
}

r := fn()