struct general_error {
	message:string
}

sum result {
	value:int
	| error:general_error
}

t:= @result.error with { message = "No go"}

match(t)
	value {
		print("Value: " + value)
	}
	| error {
		print("Error: " + error.message)
	}
